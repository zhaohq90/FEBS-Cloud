spring:
  application:
    name: FEBS-Auth
  cloud:
    nacos:
      config:
        server-addr: 116.62.174.104:8848
        group: DEFAULT_GROUP
        prefix: febs-auth
        file-extension: yaml
        enabled: false
      discovery:
        server-addr: 116.62.174.104:8848
  thymeleaf:
    cache: false

logging:
  level:
    org:
      springframework:
        boot:
          actuate:
            endpoint:
              EndpointId: error
    com:
      alibaba:
        cloud:
          nacos:
            client:
              NacosPropertySourceBuilder: error
